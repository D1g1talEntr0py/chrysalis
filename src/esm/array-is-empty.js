/**
 * Checks to see if the array is empty
 *
 * @param {Array<*>} array The array to check.
 * @returns {boolean} true if the array is empty, false otherwise.
 */
const _arrayIsEmpty = (array) => array?.length == 0;

export default _arrayIsEmpty;